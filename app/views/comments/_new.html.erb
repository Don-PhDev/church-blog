<%= form_with model: [ post, post.comments.build ] do |form| %>
  <%= form.text_area :body, size: "30x3", placeholder: "Add a comment..." %>
  <%= form.submit class: "btn btn-primary btn-sm" %>
<% end %>
